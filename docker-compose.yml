services:
  display-bps:
    build: .
    container_name: display-bps
    restart: unless-stopped
    ports:
      - "7075:7075"
    environment:
      HOST: 0.0.0.0
      PORT: 7075
    volumes:
      - ./config.json:/app/config.json
